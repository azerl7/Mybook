<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.geo.mybook.user.biz.mapper.UserMapper">
    <update id="updateByIdSelective" parameterType="com.geo.mybook.user.biz.domain.po.User">
        UPDATE user
        <set>
            <!-- 只更新非空字段，使用if标签进行判断 -->
            <if test="nickname != null and nickname != ''">
                nickname = #{nickname},
            </if>
            <if test="avatar != null and avatar != ''">
                avatar = #{avatar},
            </if>
            <if test="sex != null">
                sex = #{sex},
            </if>
            <if test="birthday != null">
                birthday = #{birthday},
            </if>
            <if test="backgroundImg!=null and backgroundImg!=''">
                background_img=#{backgroundImg},
            </if>
            <if test="introduction!=null and introduction!=''">
                introduction=#{introduction},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        WHERE id = #{id}
    </update>

    <select id="selectByAccount" resultType="com.geo.mybook.user.biz.domain.po.User">
        select id,password from user
        where ${column} = #{account}
    </select>
    <select id="selectById2nickname2avatar2introduction" resultType="com.geo.mybook.user.biz.domain.po.User">
        select nickname,avatar,introduction
        from user
        where id=#{id};
    </select>

    <select id="selectByIds" resultType="com.geo.mybook.user.biz.domain.po.User">
        select id,nickname,avatar,introduction
        from user
        where status=0 and is_deleted=0
        and id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>
    <select id="selectByIdMybatis" resultType="com.geo.mybook.user.biz.domain.po.User">
        select *
        from user
        where id=#{id} and status = 0 and is_deleted= 0
    </select>

</mapper>