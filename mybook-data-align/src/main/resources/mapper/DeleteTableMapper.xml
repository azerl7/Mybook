<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.geo.mybook.data.align.mapper.DeleteTableMapper">

    <delete id="deleteDataAlignFollowingCountTempTable" parameterType="map">
        DROP TABLE IF EXISTS `data_align_following_count_temp_${tableNameSuffix}`;
    </delete>

    <delete id="deleteDataAlignFansCountTempTable" parameterType="map">
        DROP TABLE IF EXISTS `data_align_fans_count_temp_${tableNameSuffix}`;
    </delete>

    <delete id="deleteDataAlignNoteCollectCountTempTable" parameterType="map">
        DROP TABLE IF EXISTS `data_align_note_collect_count_temp_${tableNameSuffix}`;
    </delete>

    <delete id="deleteDataAlignUserCollectCountTempTable" parameterType="map">
        DROP TABLE IF EXISTS `data_align_user_collect_count_temp_${tableNameSuffix}`;
    </delete>

    <delete id="deleteDataAlignUserLikeCountTempTable" parameterType="map">
        DROP TABLE IF EXISTS `data_align_user_like_count_temp_${tableNameSuffix}`;
    </delete>

    <delete id="deleteDataAlignNoteLikeCountTempTable" parameterType="map">
        DROP TABLE IF EXISTS `data_align_note_like_count_temp_${tableNameSuffix}`;
    </delete>

    <delete id="deleteDataAlignNotePublishCountTempTable" parameterType="map">
        DROP TABLE IF EXISTS `data_align_note_publish_count_temp_${tableNameSuffix}`;
    </delete>

    <delete id="batchDeleteDataAlignFollowingCountTempTable" parameterType="list">
        delete from `data_align_following_count_temp_${tableNameSuffix}`
        where user_id in
        <foreach collection="userIds" open="(" item="userId" close=")" separator=",">
            #{userId}
        </foreach>
    </delete>
    <delete id="batchDeleteDataAlignNoteLikeCountTempTable" parameterType="list">
        delete from `data_align_note_like_count_temp_${tableNameSuffix}`
        where note_id in
        <foreach collection="noteIds" open="(" item="noteId" close=")" separator=",">
            #{noteId}
        </foreach>
    </delete>
</mapper>